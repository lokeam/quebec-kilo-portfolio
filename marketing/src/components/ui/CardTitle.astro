---
import { cn } from '@/utils/cn';

export interface Props {
  className?: string;
}

const { className = "" } = Astro.props;
---

<h3 class={cn("text-lg font-semibold text-white py-2", className)}>
  <slot />
</h3>